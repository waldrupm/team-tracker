<h2 class="text-xl font-semibold text-indigo-800 mb-4">Teams List</h2>
<% @players.each do |player| %>
  <div>
    <h3><%= link_to player.name, player_path(player) %></h3>
    <p><%= player.drafted ? 'Yes' : 'No' %></p>
    <p><%= player.rating %></p>
  </div>
<% end %>
